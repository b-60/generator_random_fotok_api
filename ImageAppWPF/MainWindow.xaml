<Window x:Class="ImageAppWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Генератор картинок (Версия 2)" 
        Height="500" Width="600"
        WindowStartupLocation="CenterScreen"
        Background="#F0F0F0">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Vertical" HorizontalAlignment="Center" Margin="0,0,0,20">
            <TextBlock Text="Лабораторная №1: Случайная картинка" 
                       FontSize="20" FontWeight="Bold" 
                       HorizontalAlignment="Center" Margin="0,0,0,10"
                       Foreground="#333"/>

            <Button x:Name="BtnLoad" Content="📷 Показать случайную картинку" 
                    Click="BtnLoad_Click" 
                    Padding="15,10" FontSize="14" Cursor="Hand"
                    Background="#007ACC" Foreground="White" BorderThickness="0"/>

            <TextBlock x:Name="TxtStatus" Text="Ожидание действия..." 
                       HorizontalAlignment="Center" Margin="0,10,0,0" 
                       Foreground="Gray" FontStyle="Italic"/>
        </StackPanel>
        <Border Grid.Row="1" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="8" Background="White" Margin="0,0,0,20">
            <Image x:Name="ImgDisplay" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,15">
            <Button x:Name="BtnDownload" Content="💾 Скачать картинку на диск" 
                    Click="BtnDownload_Click" 
                    Padding="15,10" FontSize="14" Cursor="Hand"
                    Background="#28A745" Foreground="White" BorderThickness="0"
                    IsEnabled="False" Visibility="Collapsed"/>
        </StackPanel>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="Версия 2.0 | Автор работы: " FontWeight="Bold" Foreground="#555"/>
            <TextBlock Text="B-60" Foreground="#007ACC" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>